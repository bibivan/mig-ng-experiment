import { InvalidControlFocusDirective } from './invalid-control-focus.directive'

describe('InvalidControlFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidControlFocusDirective()
    expect(directive).toBeTruthy()
  })
})
